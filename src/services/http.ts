export const requestInit = {
    baseURL: 'http://gateway.marvel.com/v1/public',
    headers: {
        method: "GET",
        headers: {
          Accept: "*/*",
        },
        // cache: "force-cache",
    }
}